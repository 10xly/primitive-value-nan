"use strict"

var reflectGetProto = require("reflect.getprototypeof-x")
var originalGetProto = require("object.getprototypeof-x")

var getDunderProto = require("dunder-proto-x/get")

var $TypeError = require("es-error-intrinsics/TypeError")

module.exports = reflectGetProto
  ? function getProto(O) {
      return reflectGetProto(O)
    }
  : originalGetProto
  ? function getProto(O) {
      if (!O || (typeof O !== "object" && typeof O !== "function")) {
        throw new $TypeError("getProto: not an object")
      }
      return originalGetProto(O)
    }
  : getDunderProto
  ? function getProto(O) {
      return getDunderProto(O)
    }
  : null
